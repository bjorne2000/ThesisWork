import React from 'react'
import Grid from '@mui/material/Grid';

function EmptyGrid(props) {
  return (
    <Grid item md={props.propMd} sm={props.propSm}>

    </Grid>
  )
}

export default EmptyGrid